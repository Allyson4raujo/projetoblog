<h4>Categorias: </h4>

{{#each categoria}}
    <hr>
    <h3>{{nome}}</h3>
    <big>{{slug}}</big>
    <a href="/categorias/{{slug}}"><button type="button" class="btn btn-secondary float-end">Ver linkados</button></a>
    <hr>
{{else}}
    <p>Não existe nenhuma categoria</p>
{{/each}}